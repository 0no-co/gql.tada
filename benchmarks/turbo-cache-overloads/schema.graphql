type Query {
  node(id: ID!): Node
  item(id: ID!): Item
}

interface Node {
  id: ID!
  name: String!
  labels: [Label!]!
}

type Label {
  id: ID!
  key: String!
  value: String
}

type NodeA implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeB implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeC implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeD implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeE implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeF implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeG implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeH implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeI implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeJ implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeK implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeL implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeM implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeN implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeO implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeP implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeQ implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeR implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeS implements Node { id: ID!, name: String!, labels: [Label!]! }
type NodeT implements Node { id: ID!, name: String!, labels: [Label!]! }

interface Item {
  id: ID!
  name: String!
}

type ItemA implements Item { id: ID!, name: String! }
type ItemB implements Item { id: ID!, name: String! }
type ItemC implements Item { id: ID!, name: String! }
type ItemD implements Item { id: ID!, name: String! }
type ItemE implements Item { id: ID!, name: String! }
type ItemF implements Item { id: ID!, name: String! }
type ItemG implements Item { id: ID!, name: String! }
type ItemH implements Item { id: ID!, name: String! }
type ItemI implements Item { id: ID!, name: String! }
type ItemJ implements Item { id: ID!, name: String! }
